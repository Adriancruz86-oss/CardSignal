"use client";
import { useEffect,useState } from "react";
type Offer={source:string;title:string;price:number;shipping:number;deliveredPrice:number;url:string;inStock:boolean;sourceType:"retailer"|"marketplace"};
type Link={source:string;url:string};
type Data={ok:boolean;checkedAt?:string;msrp?:number|null;liveOffers?:Offer[];bestLiveOffer?:Offer|null;atOrBelowMsrp?:boolean;savingsVsMsrp?:number|null;retailerSearchLinks?:Link[];configuredSources?:Record<string,boolean>;note?:string;error?:string};
function money(v:number){return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(v)}
export default function RetailRadar({query,msrp,compact=false}:{query:string;msrp?:number|null;compact?:boolean}){const[data,setData]=useState<Data|null>(null);const[loading,setLoading]=useState(true);useEffect(()=>{let live=true;const p=new URLSearchParams({q:query});if(msrp!=null)p.set("msrp",String(msrp));fetch(`/api/sealed-retail-radar?${p}`,{cache:"no-store"}).then(r=>r.json()).then(j=>live&&setData(j)).catch(e=>live&&setData({ok:false,error:e instanceof Error?e.message:"Retail lookup failed"})).finally(()=>live&&setLoading(false));return()=>{live=false}},[query,msrp]);if(loading)return <div className={`retail-radar ${compact?"compact":""}`}>Checking retail…</div>;if(!data?.ok)return <div className={`retail-radar ${compact?"compact":""}`}>Retail unavailable</div>;const best=data.bestLiveOffer;return <div className={`retail-radar ${compact?"compact":""}`}><div className="rr-head"><span>RETAIL RADAR</span>{data.atOrBelowMsrp&&<b>AT/BELOW MSRP</b>}</div>{best?<a className="rr-best" href={best.url} target="_blank" rel="noreferrer"><strong>{money(best.deliveredPrice)}</strong><span>{best.source} · verified live</span>{data.savingsVsMsrp!=null&&data.savingsVsMsrp>0&&<small>{money(data.savingsVsMsrp)} under MSRP</small>}</a>:<div className="rr-none">No verified live retailer offer from configured sources.</div>}<div className="rr-links">{(data.retailerSearchLinks||[]).map(x=><a key={x.source} href={x.url} target="_blank" rel="noreferrer">{x.source}</a>)}</div><small className="rr-note">Verified price/stock only counts configured live sources. Other buttons open retailer searches.</small><style jsx>{`.retail-radar{margin-top:9px;padding:10px;border:1px solid rgba(90,200,230,.16);border-radius:9px;background:#04151f;color:#cce7ef}.rr-head{display:flex;justify-content:space-between;gap:8px;align-items:center}.rr-head span{font-size:8px;font-weight:900;letter-spacing:.09em;color:#6dd9ef}.rr-head b{font-size:7px;color:#7ef0a9}.rr-best{display:flex;flex-direction:column;margin-top:7px;padding:8px;border-radius:7px;background:rgba(60,180,110,.08);text-decoration:none;color:#a8f2c2}.rr-best strong{font-size:16px}.rr-best span,.rr-best small{font-size:8px;color:#7ea493}.rr-none{margin-top:7px;color:#7b969f;font-size:9px}.rr-links{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}.rr-links a{font-size:8px;text-decoration:none;color:#9ad8e8;border:1px solid rgba(100,190,220,.16);padding:5px 7px;border-radius:6px}.rr-note{display:block;margin-top:6px;color:#56717b;font-size:7px;line-height:1.4}.compact{padding:8px}.compact .rr-links a{padding:4px 6px}`}</style></div>}
