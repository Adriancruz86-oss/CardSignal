"use client";

import {useEffect,useState} from "react";
import {createPortal} from "react-dom";
import {CURATED_BENCHMARK_50} from "./curated-benchmark-cards";

type SavedCard={id:number;player:string;year?:string;setName?:string;cardNumber?:string;variant?:string;grader?:string;grade?:string;benchmark?:boolean;demo?:boolean};
const KEY="cardsignal-added-cards";
function read():SavedCard[]{try{const value=JSON.parse(localStorage.getItem(KEY)||"[]");return Array.isArray(value)?value:[]}catch{return[]}}
function identity(card:SavedCard){return[card.player,card.year,card.setName,card.cardNumber,card.variant,card.grader,card.grade].map(value=>String(value||"").trim().toLowerCase()).join("|")}
function write(cards:SavedCard[]){localStorage.setItem(KEY,JSON.stringify(cards.slice(0,500)));window.dispatchEvent(new Event("cardsignal:user-cards-changed"))}

export default function CuratedBenchmarkLayer(){
 const[ready,setReady]=useState(false),[open,setOpen]=useState(false),[message,setMessage]=useState("");
 useEffect(()=>setReady(true),[]);
 const load=()=>{if(!window.confirm("Replace existing demo and benchmark cards with the curated 50? Personal cards will be preserved."))return;const personal=read().filter(card=>!card.benchmark&&!card.demo),seen=new Set(personal.map(identity)),now=Date.now();const curated=CURATED_BENCHMARK_50.map((row,index)=>({id:now+index+1,player:row.player,year:row.year,setName:row.setName,cardNumber:row.cardNumber,league:row.league,sport:row.league,meta:`${row.year} · ${row.setName} · #${row.cardNumber} · Raw`,mode:"watching" as const,grader:"Raw",gradingCompany:"Raw",score:0,move:"NOT SCANNED",tone:"hold" as const,marketValue:0,addedAt:new Date().toISOString(),benchmark:true,benchmarkBatch:"curated-50-v1",catalogConfirmed:true,catalogSource:"CardSignal curated benchmark",canonicalIdentity:{playerName:row.player,year:row.year,setName:row.setName,cardNumber:row.cardNumber}})).filter(card=>{const key=identity(card);if(seen.has(key))return false;seen.add(key);return true});write([...curated,...personal]);setMessage(`Loaded ${curated.length} curated cards. Removed old test cards and preserved ${personal.length} personal cards.`)};
 const clear=()=>{if(!window.confirm("Remove all demo and benchmark cards? Personal cards will be preserved."))return;const rows=read(),personal=rows.filter(card=>!card.benchmark&&!card.demo);write(personal);setMessage(`Removed ${rows.length-personal.length} test cards. ${personal.length} personal cards remain.`)};
 if(!ready)return null;
 return createPortal(<><button className="cs-curated-launch" onClick={()=>setOpen(true)}>CURATED 50</button>{open&&<div className="cs-curated-backdrop" onMouseDown={event=>event.target===event.currentTarget&&setOpen(false)}><section className="cs-curated-modal"><button className="close" onClick={()=>setOpen(false)}>×</button><span>TRUSTED TEST FOUNDATION</span><h2>Curated 50-Card Portfolio</h2><p>Fifty established exact identities across MLB, NBA, NFL, and NHL. CardSignal supplies no prices, movement, scores, or signals—live scans must establish every data point.</p><div className="mix"><div><b>20</b><small>MLB</small></div><div><b>15</b><small>NBA</small></div><div><b>10</b><small>NFL</small></div><div><b>5</b><small>NHL</small></div></div><div className="examples">{CURATED_BENCHMARK_50.slice(0,8).map(card=><div key={`${card.year}-${card.setName}-${card.cardNumber}`}><strong>{card.player}</strong><small>{card.year} {card.setName} #{card.cardNumber}</small></div>)}</div>{message&&<div className="message">{message}</div>}<div className="actions"><button onClick={load}>REPLACE TEST CARDS WITH CURATED 50</button><button className="danger" onClick={clear}>CLEAR TEST CARDS</button></div><footer>Both operations preserve personal cards. Curated cards enter the watchlist as raw, unscanned benchmark identities.</footer></section></div>}<style jsx global>{`.cs-curated-launch{position:fixed;left:24px;bottom:24px;z-index:875}.cs-curated-backdrop{position:fixed;inset:0;z-index:1950;display:grid;place-items:center;padding:20px;background:rgba(0,7,12,.92)}.cs-curated-modal{position:relative;width:min(760px,96vw);max-height:92vh;overflow:auto;padding:28px;border:1px solid rgba(80,198,238,.2);border-radius:16px;background:#061724;color:#edfaff}.cs-curated-modal .close{position:absolute;right:14px;top:12px;width:34px;height:34px;border:1px solid rgba(90,190,225,.16);border-radius:8px;background:#081d2b;color:#9ab8c5;font-size:23px}.cs-curated-modal>span{color:#62e9aa;font-size:8px;font-weight:900;letter-spacing:.15em}.cs-curated-modal h2{margin:7px 0 5px;font-size:27px}.cs-curated-modal>p{margin:0;color:#829da9;font-size:10px;line-height:1.55}.cs-curated-modal .mix{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin:17px 0}.cs-curated-modal .mix div{padding:11px;border:1px solid rgba(80,190,225,.09);border-radius:8px;background:#081d2b}.cs-curated-modal .mix b,.cs-curated-modal .mix small{display:block}.cs-curated-modal .mix b{font-size:20px}.cs-curated-modal .mix small{color:#698796;font-size:7px}.cs-curated-modal .examples{display:grid;grid-template-columns:1fr 1fr;gap:5px}.cs-curated-modal .examples div{padding:8px;border-bottom:1px solid rgba(80,190,225,.07)}.cs-curated-modal .examples strong,.cs-curated-modal .examples small{display:block}.cs-curated-modal .examples strong{font-size:9px}.cs-curated-modal .examples small{margin-top:2px;color:#6f8c99;font-size:7px}.cs-curated-modal .message{margin-top:12px;padding:9px;border:1px solid rgba(98,235,169,.18);border-radius:7px;color:#91efb7;font-size:8px}.cs-curated-modal .actions{display:flex;gap:8px;margin-top:16px}.cs-curated-modal .actions button{height:38px;padding:0 12px;border:1px solid rgba(95,235,165,.28);border-radius:7px;background:rgba(49,190,124,.08);color:#9af4bd;font-size:8px;font-weight:900}.cs-curated-modal .actions .danger{border-color:rgba(255,118,135,.2);background:rgba(180,45,63,.06);color:#f4a0aa}.cs-curated-modal footer{margin-top:12px;color:#607d8a;font-size:7px;line-height:1.45}@media(max-width:620px){.cs-curated-modal .examples{grid-template-columns:1fr}.cs-curated-modal .actions{flex-direction:column}}`}</style></>,document.body);
}
